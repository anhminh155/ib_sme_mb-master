import 'package:flutter/material.dart';

class InputDecorationApp extends StatelessWidget {
  const InputDecorationApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
